services:
  # Node.js backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MOCK_DB=false
      - MONGODB_URI=mongodb+srv://palritika79_db_user:GDUK2VCoiEoXWZWJ@cluster0.s11hky4.mongodb.net/visual_route_planner?appName=Cluster0
    volumes:
      - backend_data:/app/data

  # React frontend served via Nginx
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=http://localhost:5000
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  backend_data:
